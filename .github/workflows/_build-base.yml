name: Reusable build & test base

# Internal reusable workflow shared by build.yml and release.yml.
# Extracts the ~200 lines of common logic (checkout, deps, tests with
# coverage, Docker setup, sysmodule + overlay build, dist packaging,
# artifact upload) so the two public workflows only declare their
# triggers and their release-specific tail jobs.
#
# Inputs let callers vary the few real differences:
#   - run-build:           gate the build job (build.yml skips it on PRs)
#   - default-branch:      used for the git safe-directory symbolic-ref
#   - artifact-name-zip:   artifact name for the release ZIP
#   - artifact-name-debug: artifact name for the debug symbols
#   - badge-always:        whether the coverage badge updates on every run
#                         (release.yml: true; build.yml: only on push)

on:
  workflow_call:
    inputs:
      run-build:
        description: 'Run the build & package job (false skips it).'
        type: boolean
        default: true
      default-branch:
        description: 'Fallback branch for git symbolic-ref in the build job.'
        type: string
        default: 'main'
      artifact-name-zip:
        description: 'Name of the release ZIP artifact.'
        type: string
        default: 'release-zip'
      artifact-name-debug:
        description: 'Name of the debug symbols artifact.'
        type: string
        default: 'debug-symbols'
      badge-always:
        description: 'Update the coverage badge on every run (not only push).'
        type: boolean
        default: true

permissions:
  contents: read
  packages: read

jobs:
  test:
    name: Tests & Coverage ≥ 80%
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          submodules: recursive
          fetch-depth: 0

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential g++ netcat-openbsd
          pip install gcovr
          sudo wget -qO /usr/local/bin/yq https://github.com/mikefarah/yq/releases/latest/download/yq_linux_amd64
          sudo chmod +x /usr/local/bin/yq

      - name: Run unit tests with coverage
        run: cd tests && make clean && make COVERAGE=1 test

      - name: Build integration tests with coverage
        run: cd tests/integration && make clean && make COV=1 all

      - name: Start LdnServer
        run: |
          cd tests/integration
          bash prepare-compose.sh
          docker compose -f docker-compose.yml up -d
          bash wait-for-server.sh localhost 30456 60
          sleep 2

      - name: Run integration tests with coverage
        run: |
          cd tests/integration
          INTEGRATION_COMPOSE_FILE="$(pwd)/docker-compose.yml" ./run_integration_tests localhost 30456

      - name: Tear down LdnServer
        if: always()
        run: cd tests/integration && docker compose -f docker-compose.yml down -v

      - name: Enforce per-file coverage ≥ 80%
        run: python3 scripts/check-coverage.py --min 80 --root .

      - name: Compute average coverage
        id: coverage
        run: |
          python3 scripts/check-coverage.py --min 0 --root . --json /tmp/cov.json || true
          python3 <<'PYEOF' > /tmp/avg.txt
          import json
          with open('/tmp/cov.json') as f:
              d = json.load(f)
          files = [f for f in d.get('files', []) if 'sysmodule/source/' in (f.get('file', f.get('filename', '')))]
          total_lines = 0
          total_covered = 0
          for f in files:
              lines = f.get('lines', [])
              if isinstance(lines, list):
                  total_lines += len(lines)
                  total_covered += sum(1 for l in lines if l.get('count', 0) > 0)
          avg = (total_covered / total_lines * 100) if total_lines > 0 else 0
          print(f'{avg:.1f}')
          PYEOF
          echo "percent=$(cat /tmp/avg.txt)" >> $GITHUB_OUTPUT

      - name: Update coverage badge
        if: ${{ inputs.badge-always == true || github.event_name == 'push' }}
        uses: schneegans/dynamic-badges-action@v1.8.0
        with:
          auth: ${{ secrets.GIST_BADGE_SECRET }}
          gistID: 4277995494c6874ebb238bdbc499d9d0
          filename: coverage.json
          label: coverage
          message: ${{ steps.coverage.outputs.percent }}%
          valColorRange: ${{ steps.coverage.outputs.percent }}
          maxColorRange: 100
          minColorRange: 0

  build:
    name: Build & Package
    runs-on: ubuntu-latest
    needs: test
    if: ${{ inputs.run-build }}
    container: ghcr.io/ethiquema/ryu_ldn_nx:monthly-20260701

    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          submodules: recursive
          fetch-depth: 0

      - name: Fix git safe directory
        env:
          DEFAULT_BRANCH: ${{ inputs.default-branch }}
        run: |
          git config --global --add safe.directory '*'
          git symbolic-ref HEAD refs/heads/"${GITHUB_REF_NAME:-$DEFAULT_BRANCH}" 2>/dev/null || true

      - name: Link pre-built libraries
        run: |
          # Symlink pre-built libstratosphere.a from the container image
          # so the sysmodule Makefile finds it without rebuilding libs.
          LIB_DIR="${GITHUB_WORKSPACE}/sysmodule/Atmosphere-libs/libstratosphere/lib/nintendo_nx_arm64_armv8a/release"
          PREBUILT="/opt/ryu_ldn_nx/libstratosphere/lib/nintendo_nx_arm64_armv8a/release/libstratosphere.a"

          if [ -f "$PREBUILT" ]; then
            mkdir -p "$LIB_DIR"
            ln -s "$PREBUILT" "$LIB_DIR/libstratosphere.a"
            echo "Linked pre-built libstratosphere.a from container image"
          else
            echo "::error::Pre-built libstratosphere.a not found at $PREBUILT"
            exit 1
          fi

      - name: Detach devkitPro packaging tools from LD_PRELOAD
        run: |
          for tool in npdmtool elf2nso build_pfs0 nacptool; do
            real="/opt/devkitpro/tools/bin/$tool"
            if [ ! -f "$real.real" ]; then
              mv "$real" "$real.real"
            fi
            cat > "$real" <<'WRAPPER'
          #!/bin/bash
          self="${0}.real"
          name="$(basename "$0")"
          # npdmtool fails to parse the input JSON when it lives on the
          # GitHub Actions workspace bind-mount (/__w/...) inside the
          # container, even though the file content is byte-identical to
          # what works elsewhere. Workaround: copy input to /tmp (container-
          # native fs) and rewrite arg before exec.
          if [ "$name" = "npdmtool" ] && [ -f "$1" ]; then
            staged="/tmp/$(basename "$1").$$"
            cp "$1" "$staged"
            shift
            set -- "$staged" "$@"
          fi
          exec env -u LD_PRELOAD "$self" "$@"
          WRAPPER
            chmod +x "$real"
          done

      - name: Build sysmodule + overlay + dist ZIP
        env:
          DEVKITPRO: /opt/devkitpro
        run: |
          export DEVKITPRO=/opt/devkitpro
          export DEVKITARM=$DEVKITPRO/devkitARM
          export DEVKITA64=$DEVKITPRO/devkitA64
          export PATH=$DEVKITPRO/tools/bin:$DEVKITA64/bin:$PATH

          # Copy NPDM descriptor to /tmp to avoid filesystem issues on
          # the GitHub Actions workspace bind-mount inside the container.
          # npdmtool cannot parse JSON from the /__w/... bind-mount.
          cp "${GITHUB_WORKSPACE}/sysmodule/res/app.json" /tmp/app.json

          cd ${GITHUB_WORKSPACE}/sysmodule && make -j4 all APP_JSON=/tmp/app.json
          cd ${GITHUB_WORKSPACE}/overlay && make -j4 all
          cd ${GITHUB_WORKSPACE}/sysmodule && make dist APP_JSON=/tmp/app.json

      - name: Upload release ZIP
        uses: actions/upload-artifact@v4
        with:
          name: ${{ inputs.artifact-name-zip }}
          path: ryu_ldn_nx-release.zip

      - name: Upload debug symbols
        uses: actions/upload-artifact@v4
        with:
          name: ${{ inputs.artifact-name-debug }}
          path: |
            sysmodule/ryu_ldn_nx.elf
            sysmodule/ryu_ldn_nx.nso
          if-no-files-found: warn